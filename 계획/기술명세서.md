# KNU Campus Life 기술명세서

## 1. 시스템 개요

KNU Campus Life는 강남대학교 학생을 위한 통합 캠퍼스 앱이다. 백엔드는 FastAPI 기반 REST API로 인증, 공지사항, 식단, 셔틀 시간표, 강의/과목, 시간표, 개인 일정, 챗봇, **푸시 알림** 기능을 제공한다. 프론트엔드는 React Native Expo 기반 모바일 앱으로 구성하며, 현재는 주요 기능들의 실제 API 연동이 완료된 상태이다.

## 2. 기술 스택

### 2.1 Backend

- Python 3.13
- FastAPI
- Uvicorn
- Pydantic v2
- SQLAlchemy
- Alembic
- PostgreSQL 15
- python-jose 기반 JWT
- passlib/bcrypt 기반 비밀번호 해싱
- python-dotenv
- Playwright
- BeautifulSoup4
- APScheduler
- LangChain
- HuggingFace Embeddings (`jhgan/ko-sroberta-multitask`)
- ChromaDB
- Google Generative AI (`gemini-flash-latest`)
- **httpx** (Expo 푸시 알림 전송용)

### 2.2 Frontend

- Expo SDK 54
- React Native 0.81.5
- React 19
- TypeScript
- Expo Router
- React Navigation
- TanStack Query v5
- Zustand
- NativeWind
- Axios
- Expo SecureStore
- Expo Auth Session
- **Expo Notifications / Expo Device** (푸시 알림 수신용)
- Lucide React Native / Expo Vector Icons

### 2.3 Database

주요 테이블 및 모델:

- `users`
- `notices`
- `daily_menus`
- `menu_items`
- `subjects`
- `courses`
- `course_schedules`
- `timetables`
- `timetable_courses`
- `custom_schedules`
- `chat_rooms`
- `messages`
- `buildings`
- `rooms`
- `shuttle`
- `notification_settings` (알림 허용 여부 및 Expo Push Token 저장)
- `notice_read`
- `completed_courses`
- `graduation_standards`
- `wizard_selections`

## 3. 구현 완료 API

### 3.1 Health

| Method | URL | 설명 |
|---|---|---|
| GET | `/health` | 서버 상태 확인 |
| GET | `/health/db` | DB 연결 상태 확인 |

### 3.2 Auth & User Settings

| Method | URL | 설명 |
|---|---|---|
| POST | `/auth/signup` | 회원가입 |
| POST | `/auth/login` | 로그인 및 access/refresh token 발급 |
| POST | `/auth/google` | Google ID Token 기반 로그인 엔드포인트 |
| GET | `/auth/me` | 현재 로그인 사용자 조회, 관리자 여부 포함 |
| PUT | `/auth/update` | 사용자 정보(이름, 학과, 학번 등) 수정 |
| PUT | `/auth/notification-token` | 기기의 Expo 푸시 토큰 등록/갱신 |
| GET | `/auth/notification-settings` | 알림 설정(공지, 식단, 셔틀) 조회 |
| PUT | `/auth/notification-settings` | 알림 설정 수정 |

### 3.3 Notices

| Method | URL | 설명 |
|---|---|---|
| POST | `/api/notices/crawl?max_items={n}` | 공지사항 크롤링 및 저장, 관리자 전용 |
| GET | `/api/notices/` | 공지사항 목록 조회 |
| GET | `/api/notices/?category={category}` | 카테고리 기반 공지사항 조회 |
| GET | `/api/notices/?keyword={keyword}` | 제목 키워드 기반 공지사항 검색 |
| GET | `/api/notices/{notice_id}` | 공지사항 상세 조회 |
| PUT | `/api/notices/{notice_id}` | 공지사항 수정, 관리자 전용 |
| DELETE | `/api/notices/{notice_id}` | 공지사항 삭제, 관리자 전용 |

### 3.4 Daily Menus

| Method | URL | 설명 |
|---|---|---|
| POST | `/api/daily-menus/crawl?max_items={n}` | 식단 이미지 크롤링 및 날짜별 저장, 관리자 전용 |
| GET | `/api/daily-menus/?date=YYYY-MM-DD` | 날짜별 식단 이미지 조회 |

### 3.5 Courses

| Method | URL | 설명 |
|---|---|---|
| POST | `/api/courses/import-html` | HTML 문자열 기반 강의 데이터 import, 관리자 전용 |
| POST | `/api/courses/import-html-file` | HTML 파일 업로드 기반 강의 데이터 import, 관리자 전용 |
| POST | `/api/courses/crawl-departments` | 학과별 개설강좌 크롤링 및 import, 관리자 전용 |
| GET | `/api/courses/` | 강의 목록 조회 |
| GET | `/api/courses/?keyword={keyword}` | 과목명 키워드 조회 |
| GET | `/api/courses/?department={department}` | 학과 필터 조회 |
| GET | `/api/courses/?professor={professor}` | 교수명 필터 조회 |
| GET | `/api/courses/?semester={semester}` | 학기 필터 조회 |
| GET | `/api/courses/?day_of_week={day}` | 요일 필터 조회 |
| GET | `/api/courses/?credits={credits}` | 학점 필터 조회 |
| GET | `/api/courses/?subject_code={code}` | 학수번호 필터 조회 |
| GET | `/api/courses/{course_id}` | 강의 상세 조회 |

### 3.6 Timetables

| Method | URL | 설명 |
|---|---|---|
| POST | `/api/timetables/` | 내 시간표 생성 |
| GET | `/api/timetables/` | 내 시간표 목록 조회 |
| GET | `/api/timetables/{timetable_id}` | 시간표 상세 조회, 강의/개인 일정/총 학점 포함 |
| PATCH | `/api/timetables/{timetable_id}` | 시간표 이름, 학기, 대표 여부 수정 |
| DELETE | `/api/timetables/{timetable_id}` | 시간표 삭제 |
| POST | `/api/timetables/{timetable_id}/courses` | 시간표에 강의 추가, 충돌 검사 포함 |
| DELETE | `/api/timetables/{timetable_id}/courses/{course_id}` | 시간표에서 강의 삭제 |
| POST | `/api/timetables/{timetable_id}/custom-schedules` | 개인 일정 추가, 충돌 검사 포함 |
| GET | `/api/timetables/{timetable_id}/custom-schedules` | 개인 일정 목록 조회 |
| PATCH | `/api/timetables/{timetable_id}/custom-schedules/{schedule_id}` | 개인 일정 수정, 충돌 검사 포함 |
| DELETE | `/api/timetables/{timetable_id}/custom-schedules/{schedule_id}` | 개인 일정 삭제 |

### 3.7 Chat

| Method | URL | 설명 |
|---|---|---|
| POST | `/api/chat/demo` | 인증 없는 AI 챗봇 데모 응답 |
| POST | `/api/chat/rooms` | 채팅방 생성 |
| GET | `/api/chat/rooms` | 내 채팅방 목록 조회 |
| PATCH | `/api/chat/rooms/{room_id}` | 채팅방 제목 수정 |
| DELETE | `/api/chat/rooms/{room_id}` | 채팅방 삭제 |
| POST | `/api/chat/rooms/{room_id}/messages` | 메시지 전송 및 RAG 기반 AI 응답 저장 (첫 메시지 시 제목 자동 생성) |
| GET | `/api/chat/rooms/{room_id}/messages` | 채팅방 메시지 목록 조회 |

### 3.8 Shuttle

| Method | URL | 설명 |
|---|---|---|
| POST | `/api/shuttles/crawl` | 셔틀 시간표 PDF 링크 크롤링 및 저장, 관리자 전용 |
| GET | `/api/shuttles/` | 셔틀 시간표 목록 조회 |
| GET | `/api/shuttles/?semester={semester}` | 학기 기반 셔틀 시간표 조회 |
| GET | `/api/shuttles/latest` | 최신 셔틀 시간표 조회 |

## 4. 주요 구현 구조

### 4.1 인증 및 권한

- 일반 이메일/비밀번호 회원가입과 로그인을 제공한다.
- 로그인 성공 시 access token과 refresh token을 발급한다.
- `/auth/me`는 현재 사용자 정보와 `is_admin` 값을 반환한다.
- 사용자 정보 수정 및 알림 설정 API가 구현되어 있다.

### 4.2 공지사항

- 개선된 `notice_crawler_v3`를 통해 본문 내 이미지까지 수집한다.
- 새 공지 수집 시 **해당 카테고리 알림을 허용한 사용자들에게 푸시 알림을 즉시 발송**한다.
- 공지사항 목록은 무한 스크롤 및 카테고리 필터를 지원한다.

### 4.3 식단

- 주간 식단표 이미지를 크롤링하여 날짜별로 저장한다.
- 새로운 식단표가 업데이트되면 **알림 허용 사용자들에게 푸시 알림을 발송**한다.

### 4.4 셔틀 시간표

- 최신 셔틀 시간표 PDF 링크를 수집하고 RAG 문서로 인덱싱한다.
- 새 시간표 등록 시 **푸시 알림을 발송**한다.

### 4.5 RAG 챗봇 및 채팅방 관리

- **채팅방 관리**: 채팅방 생성, 삭제, 이름 수정 기능이 구현되어 있으며, Drawer 메뉴에서 Long Press로 설정 모달을 띄울 수 있다.
- **자동 제목 생성**: 새 채팅방에서 첫 질문을 던지면 질문 내용의 앞부분을 따서 채팅방 제목을 자동으로 설정한다.
- **Empty State**: 채팅 메시지가 없을 때 추천 질문을 제안하여 대화를 유도한다.
- 공지사항, 셔틀, 학사일정, 학교 규정 데이터를 기반으로 답변한다.

### 4.6 푸시 알림 (Push Notification)

- **Backend**: Expo Push API를 사용하여 `NotificationSetting` 테이블에 저장된 토큰으로 알림을 전송한다. 크롤러 작업 완료 시 `notify_users_by_type` 함수를 호출한다.
- **Frontend**: 앱 실행 시 `expo-notifications`를 통해 권한을 요청하고 토큰을 획득하여 서버에 등록한다. 알림 클릭 시 해당 화면으로 이동하는 로직을 갖춘다.

### 4.7 Scheduler

- APScheduler를 통해 공지사항(1시간), 학식(매일 새벽 1시), 셔틀(12시간) 크롤링을 정기적으로 수행한다.

## 5. 검증 완료 항목

- 서버 및 DB health check
- 회원가입, 로그인, JWT 인증 및 사용자 정보 수정
- **알림 설정 조회/수정 및 푸시 토큰 등록 API**
- 공지사항 크롤링 (이미지 포함) 및 조회
- 식단/셔틀 크롤링 및 조회
- 강의/과목 전 학과 자동 수집 및 시간표 충돌 검사 시스템
- **채팅방 CRUD (생성/조회/수정/삭제) 및 UI(Drawer, Long Press 모달)**
- **채팅방 첫 질문 기반 자동 제목 생성 및 가이드 UI**
- **공지사항, 식단, 셔틀 업데이트 시 푸시 알림 자동 발송**
- RAG 기반 챗봇 (학사일정/규정/공지/셔틀 데이터 인덱싱 완료)
- APScheduler 기반 정기 크롤링 구조

## 6. 남은 고도화 과제

- Google 로그인 백엔드 로직 검증 및 보완
- 공지사항 읽음 상태 API 구현
- 졸업 요건, 기이수 과목, 위저드 선택 관련 실제 데이터 연동
- 건물/강의실 상세 조회 API 구현
- 관리자 전용 데이터 관리 대시보드 UI

## 7. Frontend 디렉터리 구조

```text
frontend/knu/
├── app/
│   ├── (app)/
│   │   ├── _layout.tsx (푸시 토큰 등록 로직 포함)
│   │   └── (tabs)/
│   │       ├── index.tsx
│   │       ├── notice.tsx
│   │       ├── schedule.tsx
│   │       ├── ai_chat.tsx (Drawer 및 설정 모달 포함)
│   │       ├── my.tsx (알림 설정 토글 포함)
...
├── src/
│   ├── api/
│   ├── components/
│   ├── features/
│   ├── hooks/
│   └── utils/
│       └── notification.ts (푸시 알림 유틸리티)
```
